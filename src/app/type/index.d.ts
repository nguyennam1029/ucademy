type TActiveLinksProps = {
  url: string;
  children: React.ReactNode;
  icon?: React.ReactNode;
}
type TMenuItem = {
     url: string;
  title: string;
  icon: React.ReactNode;
}
export {
    TActiveLinksProps,
    TMenuItem
}